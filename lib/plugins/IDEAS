decompose assign
macro
extend_for
slices
question_mark
common js


push_down_tree

x = (if cond
      5
    else if cond2
      if cond3
        7)

brackets_as_closures
(x
y)
if more than 1 expression - add in closure
(function() {
  x
  y
})
with implicit return
(function() {
  x
  return y
})

implicit_returns

assigning_blocks_as_closure

if assignment to if - add in brackets


#kaffeine indented_blocks
if(true)
  do_something()
  and_somemore()
    and(some(more))
else
  do_more
  for(x in A)
    send_for_help()
    etc
    etc
  if(true)
    sayhello()
    saygoodbye()
  
function(x)
  return y
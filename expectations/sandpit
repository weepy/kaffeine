compiles:
Puzzle = require("./puzzle")



User = require("mmmodel")["redis"]("User", {
  id: {"type":"string", required: true }
})
 
console.log(require("./puzzle") == Puzzle)

User::puzzles = (type, callback) {
  console.log(require("./puzzle") == Puzzle)
  callback()
  // callback this.find_edges(type).load_as(Puzzle).all!
}

module.exports = User

to:
x

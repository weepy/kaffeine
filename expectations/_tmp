
compiles:
for file of x("fs").y "#{a}/#{x}" {
  xxxxx
}

to:

Unit.prototype.inactive = function() {}

x

  
var _xpr, _a, file; for(_a = 0; _a < ((_xpr=x("fs").y)(("" + (a) + "/" + (x)))).length; _a++) { file = _xpr[_a];
  xxxxx
}

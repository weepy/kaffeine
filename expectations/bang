#bang
compiles:
sum = add!(3, 4)
ok(sum == 7)
to:
add(3, 4, function(sum) {
  ok(sum == 7)    
})

compiles:

nested!()
nested!()
nested!(1, 2)
x
to:

nested(function() {
  nested(function() {
    nested(1, 2, function() {
      x
    })
  })
})

